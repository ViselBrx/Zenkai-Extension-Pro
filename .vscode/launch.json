{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Zenkai Pro (Free + Pro)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/..",
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/../out/**/*.js",
        "${workspaceFolder}/out/**/*.js"
      ],
      "env": {
        "ZENKAI_DODO_ENVIRONMENT": "test_mode"
      }
    }
  ]
}
